name: microalgae_system_app # This is the missing line that caused the error!
description: A mobile app for microalgae cultivation monitoring.
publish_to: 'none' # Ensures you cannot accidentally publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0  # Required for initialization
  cloud_firestore: ^5.0.0 # Required for your FirestoreService
  firebase_messaging: ^15.0.0

  # New dependency added to support multi-listener streams for the dashboard fix
  rxdart: ^0.27.7 

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
